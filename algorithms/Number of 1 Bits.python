class Solution(object):
    def hammingWeight(self, n):
        """
        :type n: int
        :rtype: int
        """
        str="{0:b}".format(n)
        ans=0
        for i in str:
            if i=="1":
                ans+=1
        return ans